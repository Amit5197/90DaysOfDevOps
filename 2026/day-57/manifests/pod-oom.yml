apiVersion: v1
kind: Pod
metadata:
  name: oom-pod-test
spec:
  containers:
    - name: ood-pod
      image: polinux/stress
      resources:
        limits:
          memory: "100Mi"
      command: ["stress"]
      args: ["--vm", "1", "--vm-bytes", "200M", "--vm-hang", "1"]
